This readme file and the example file are added to your save directory when the overlay is loaded.

To change axis or key mapping, rename (or copy) keys.conf.example to keys.conf in your save directory.

To view button and axis numbers, use the 'z' key to toggle joystick information on.